import { Typography } from '@mui/material';
import { EditorProps } from '@/pages/Edit/editors/index';

export default function EditorResult({ id }: EditorProps) {
  return (
    <>
      <Typography variant={'h4'}>Результаты</Typography>
    </>
  );
}
